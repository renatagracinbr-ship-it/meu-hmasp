# ============================================================================
# HMASP CHAT - Configurações de Ambiente
# ============================================================================
# Copie este arquivo para .env e preencha com suas credenciais
# Comando: cp .env.example .env

# ============================================================================
# SERVIDOR NODE.JS
# ============================================================================
PORT=3000
NODE_ENV=production

# ============================================================================
# BANCO DE DADOS POSTGRESQL - AGHUse
# ============================================================================
DB_HOST=10.12.40.105
DB_PORT=5432
DB_USER=aghuse
DB_PASSWORD=sua_senha_aqui
DB_NAME=agh

# Pool de conexões
DB_POOL_MIN=2
DB_POOL_MAX=10

# ============================================================================
# WHATSAPP - CONFIGURAÇÃO DA API
# ============================================================================
# Modo da API: 'official' (Meta Cloud API) ou 'unofficial' (whatsapp-web.js)
WHATSAPP_API_MODE=unofficial

# ----------------------------------------------------------------------------
# API NÃO OFICIAL (whatsapp-web.js) - Modo: unofficial
# ----------------------------------------------------------------------------
# Caminho da sessão do WhatsApp
WHATSAPP_SESSION_PATH=./server/.wwebjs_auth

# Nome da sessão do WhatsApp (para múltiplas instâncias)
WHATSAPP_SESSION_NAME=hmasp-chat-session

# Timeout de mensagens (em segundos)
WHATSAPP_MESSAGE_TIMEOUT=30

# ----------------------------------------------------------------------------
# API OFICIAL (Meta Cloud API) - Modo: official
# ----------------------------------------------------------------------------
# Token de acesso permanente (gerado no Meta Business Suite)
WHATSAPP_ACCESS_TOKEN=seu_token_aqui

# Phone Number ID (ID do número de telefone verificado)
WHATSAPP_PHONE_NUMBER_ID=seu_phone_number_id_aqui

# WhatsApp Business Account ID
WHATSAPP_BUSINESS_ACCOUNT_ID=seu_waba_id_aqui

# URL do webhook para receber mensagens (obrigatório para API oficial)
WHATSAPP_WEBHOOK_URL=https://seu-servidor.com/webhook/whatsapp

# Token de verificação do webhook (você define)
WHATSAPP_WEBHOOK_VERIFY_TOKEN=seu_token_verificacao_aqui

# ============================================================================
# AUTENTICAÇÃO
# ============================================================================
# Método de autenticação (local_files, database, etc)
AUTH_METHOD=local_files

# Tempo de expiração de sessão (em dias)
SESSION_EXPIRY_DAYS=7

# Habilitar auto-login para VM (true/false)
AUTO_LOGIN_ENABLED=true

# ============================================================================
# LOGS E MONITORAMENTO
# ============================================================================
LOG_LEVEL=info
# Opções: error, warn, info, debug

# Habilitar logs de auditoria
ENABLE_AUDIT=true

# ============================================================================
# URLs DO SISTEMA
# ============================================================================
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3000

# ============================================================================
# LGPD E PRIVACIDADE
# ============================================================================
# Tempo de retenção de dados em dias
DATA_RETENTION_DAYS=30

# Habilitar anonimização de dados antigos
ENABLE_ANONYMIZATION=true

# ============================================================================
# OPCIONAL - CORS
# ============================================================================
# Se precisar configurar CORS específico
# CORS_ORIGIN=http://localhost:3000
